# Upload instructions

Upload everything inside this folder to the root of `Innocent254/wuwa-companion-unofficial`.

Suggested commit message:

```text
Build responsive multilingual UI foundation
```

After upload, run the Android workflow in `build-only` mode before configuring release signing.
